services:
  app:
    build:
      context: ..
      dockerfile: .dockerdev/Dockerfile.dev
    working_dir: /app
    environment:
      - TZ=${TZ:-Europe/Moscow}
    ports:
      - '4000:4000'
    volumes:
      - ..:/app
      - bundle:/usr/local/bundle

volumes:
  bundle:
