FROM ruby:3.3-slim

WORKDIR /app

RUN apt-get update -qq \
  && apt-get install -y --no-install-recommends \
    build-essential \
    git \
    curl \
    ca-certificates \
    imagemagick \
    webp \
  && rm -rf /var/lib/apt/lists/*

CMD ["sleep", "infinity"]
